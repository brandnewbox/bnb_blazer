source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in bnb-blazer.gemspec.
gemspec

group :development do
  gem "rails", "~> 6.1"
  gem "puma"
  gem "pg"
  gem "faker"
  gem 'airbrake'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
